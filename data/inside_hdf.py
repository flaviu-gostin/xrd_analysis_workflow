#hdf files are organised like filesystems they have groups (equivalent to
#directories) and datasets.  The dataset 'data' is inside the group 'detector',
#which is inside the group 'instrument', which ...  Varible(s) in this script
#is (are) called by other scripts in the project.
hdf_dataset_path = 'entry/instrument/detector/data'
